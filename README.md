# ModalPhone

Custom modal web component for set phone number.

## Features

- Allows the user to input a phone number.
- Validates the phone number format.
- Supports custom resolution callback.
- Easy to integrate with your web application.

## Installation

To install in npm:

```bash
npm install @vkruychkov/phone-service